# @types/node 等依赖由 scripts/build.sh 从 DSH checkout 以 junction 链接进来，
# registry 中不存在其传递依赖（@deepseek-ai/dsh-type-meta）——关闭 pnpm 运行前依赖自检，
# 否则 `pnpm test` 会在 install 阶段 404 失败。
verify-deps-before-run=false
